# Основные библиотеки
pandas>=2.0.0
openpyxl>=3.1.0
python-docx>=0.8.11
tqdm>=4.65.0
numpy>=1.24.0
sentencepiece

# Web UI
Flask>=2.0.0
protobuf

# LLM - GPU оптимизированные библиотеки
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
bitsandbytes>=0.41.0  # Для 4-bit quantization
PyYAML>=6.0  # Для чтения config.yaml

# Опционально: vllm для еще большей оптимизации (требует CUDA)
# vllm>=0.2.0  # Раскомментируйте если хотите использовать vllm

# PDF parsing
pdfplumber>=0.7.6

# Опциональные для улучшения
XlsxWriter>=3.1.0

# Старые зависимости (можно удалить если не используются)
# llama-cpp-python>=0.2.20  # Оставлено для обратной совместимости
